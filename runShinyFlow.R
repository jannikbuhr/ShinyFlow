# If you don't have the following packages installed, do so with 'install.packages(...)'
# shiny, shinydashboard, tidyverse, DT
#
# then run these two lines

library(shiny)
runGitHub("ShinyFlow", "jannikbuhr")
